package com.chanakya.shl2.model.enums;

public enum ShlFlag {
    L, // Long-term link (data may change over time)
    P, // Passcode protected
    U  // Direct file access (single file, GET instead of POST)
}
